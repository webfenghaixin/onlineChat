# 制图页面（DrawPage）结构修改文档
> 针对PC端桌面视图，结合暗黑主题参考风格进行的结构优化梳理

---

## 一、当前 DrawPage 结构概览

### 1.1 当前页面组成（移动端优先设计）

```
┌───────────────────────────────────────────────────┐
│  Drawer 抽屉式侧栏（覆盖层，默认关闭）               │
│  ┌─────────────────────────────────────────────┐  │
│  │  Tabs: [画图历史] | [设置]                  │  │
│  │  - 历史会话列表（Card组件）                  │  │
│  │  - 字体大小等设置                           │  │
│  └─────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
          ↕ 点击 ☰ 按钮打开/关闭

┌───────────────────────────────────────────────────┐
│  Header（三列式）                                  │
│  [☰ 打开侧栏]  [标题+生成状态]  [返回聊天]         │
├───────────────────────────────────────────────────┤
│  BalanceBar（余额条）                              │
│  [余额信息]                    [充值按钮]          │
├───────────────────────────────────────────────────┤
│                                                   │
│  消息列表区域（纵向滚动）                           │
│  - 用户消息（含参考图+提示词+配置标签）             │
│  - AI结果（图片网格+摘要+操作按钮）                 │
│  - 空状态引导卡片                                  │
│                                                   │
├───────────────────────────────────────────────────┤
│  悬浮底部输入区（脱离文档流，absolute定位）         │
│  ┌─────────────────────────────────────────────┐  │
│  │ 等待任务条（异步生成提示）                    │  │
│  ├─────────────────────────────────────────────┤  │
│  │ 参考图待上传卡片列表                         │  │
│  ├─────────────────────────────────────────────┤  │
│  │ 绘图参数（Collapse折叠面板，默认收起）        │  │
│  │ ▷ 绘图参数  模型 · 尺寸 · 数量摘要          │  │
│  │ ▾ 展开后：模型/模式/尺寸/质量/数量 横向滚动  │  │
│  ├─────────────────────────────────────────────┤  │
│  │ 📎 提示词输入textarea            [🎨 生成]   │  │
│  └─────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
```

### 1.2 现存结构特征
| 特征 | 说明 |
|------|------|
| 侧栏 | 抽屉式（Drawer），默认隐藏，需点击打开，带背景遮罩 |
| Header | 3列网格布局，居中显示标题，返回按钮在右侧 |
| 余额展示 | 独立 BalanceBar 组件，占一行垂直空间 |
| 参数配置 | 默认折叠在底部输入区，需手动点开查看/修改 |
| 布局容器 | `phone-shell` 类，带圆角和阴影（即使PC端也是"手机壳"效果） |
| 输入区 | 悬浮绝对定位在底部，消息列表靠 padding-bottom 预留高度 |
| 滚动收起 | 支持滚动时收起输入框，显示FAB按钮展开 |

---

## 二、PC端目标结构（参考暗黑风格AI绘图工具）

### 2.1 行业参考（Midjourney / SD WebUI / DALL·E 共同特征）
1. **三栏式固定布局**：左（历史） + 中（结果展示） + 下（输入配置）
2. **参数面板常驻**：模型、尺寸、质量等配置始终可见，不用点开
3. **侧栏持久化**：历史会话不收起，始终可点击切换
4. **图片更突出**：结果展示区占主要面积，网格列数根据宽度自适应
5. **紧凑操作**：Header合并余额、快捷操作、设置

### 2.2 目标PC制图页布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│  顶部导航栏（固定高度 ~56px）                                                  │
│  ┌──────────┬──────────────────────────────┬───────────────────────────────┐ │
│  │ [← 返回] │  🎨 画图会话标题              │  [🌞/🌙] [余额: N次] [充值]   │ │
│  └──────────┴──────────────────────────────┴───────────────────────────────┘ │
├──────────────┬───────────────────────────────────────────────────────────────┤
│              │  次工具栏（可选 ~40px）                                        │
│              │  [+ 新建画图]  [✓ 批量管理]  [⬇ 下载全部]    [🔎 视图切换]    │
│              ├───────────────────────────────────────────────────────────────┤
│  左侧栏      │                                                               │
│  (固定宽度)  │                                                               │
│  280~320px   │              中间结果展示区（自适应，占据剩余空间）            │
│              │                                                               │
│  ┌────────┐  │              - 消息流纵向排列                                 │
│  │🔍 搜索 │  │              - 用户提示词卡片（浅色/深色气泡）                 │
│  ├────────┤  │              - AI生成结果卡片：                               │
│  │+ 新建  │  │                 ┌────────┬────────┬────────┐                  │
│  ├────────┤  │                 │ 图片1  │ 图片2  │ 图片3  │  网格布局       │
│  │ 会话历史│  │                 └────────┴────────┴────────┘                  │
│  │ [Tab]  │  │                 [进度摘要条] [4/4完成] [用时XX秒]             │
│  │        │  │                 [↻ 再次生成] [📋 复制提示词] [🗑 删除]        │
│  │ - 会话1 │  │                                                               │
│  │ ✓ 当前  │  │              - 空状态：插画+提示引导                          │
│  │ - 会话2 │  │                                                               │
│  │ - 会话3 │  │                                                               │
│  │ ...    │  │                                                               │
│  │        │  │                                                               │
│  ├────────┤  │                                                               │
│  │ 设置Tab│  │                                                               │
│  │ - 主题 │  │                                                               │
│  │ - 字体 │  │                                                               │
│  └────────┘  │                                                               │
├──────────────┴───────────────────────────────────────────────────────────────┤
│                                                                              │
│  底部配置输入区（固定高度，可展开高级参数，~180~260px）                         │
│  ┌──────────────────────────────────────────────────────────────────────────┐ │
│  │ 参考图上传区（已选参考图缩略图横排 + 拖拽/点击上传入口）                    │ │
│  ├──────────────────────────────────────────────────────────────────────────┤ │
│  │ 参数配置区（多选项网格，**默认展开，不折叠**）                               │ │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌───────────┐ │ │
│  │  │ 模型:  │ │ 模式:  │ │ 尺寸:  │ │ 质量:  │ │ 数量:  │ │ 更多 ▾    │ │ │
│  │  │ Select │ │ Select │ │ Select │ │ Select │ │ Select │ │           │ │ │
│  │  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘ └───────────┘ │ │
│  ├──────────────────────────────────────────────────────────────────────────┤ │
│  │ 📎  提示词输入框（多行，自适应高度，最小~80px，支持Ctrl+Enter发送）        │ │
│  │                                                                          │ │
│  ├──────────────────────────────────────────────────────────────────────────┤ │
│  │ 异步生成等待提示条（如有任务进行中）   [↻ 重试失败] [🎨 生成 (Shift+Enter)]│ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、具体修改点清单

### 3.1 布局容器层

| # | 修改项 | 当前 | 目标 | 说明 |
|---|--------|------|------|------|
| 1 | 外层容器 | `phone-shell`（圆角+阴影+最大宽限制） | `desktop-draw-shell`（全屏无边框，无圆角，无最大宽） | PC端去掉"手机壳"视觉 |
| 2 | 页面布局 | 单列流式（Header + 余额 + 列表 + 悬浮输入） | 三区域固定布局：左栏+中内容+底输入 | 使用CSS Grid：`grid-template-areas` |
| 3 | 高度计算 | 100vh + 动态padding | 100vh 精确划分：Header(56px) + 次工具栏(40px) + 剩余自适应 + 底输入(200px) | 避免任何外部滚动条，内部各区域独立滚动 |

### 3.2 顶部导航栏（Header）

| # | 修改项 | 当前 | 目标 | 说明 |
|---|--------|------|------|------|
| 1 | 余额展示 | 独立BalanceBar组件，占整行 | 合并到Header右侧，紧凑型 | 省出垂直空间 |
| 2 | 主题切换入口 | 无 | Header右侧增加图标按钮（🌞/🌙） | 快捷切换主题 |
| 3 | 左按钮 | ☰ 打开抽屉侧栏 | ← 返回聊天（返回功能明确） | PC端侧栏常驻，无需"打开侧栏"按钮 |
| 4 | 中间标题 | 会话标题 + 状态点 | 会话标题 + 更紧凑的状态文字 | 状态："已存N张"/"N个任务生成中" |
| 5 | 右按钮 | 返回聊天 | 余额 + 充值 + 主题切换 | 多按钮水平排列 |

### 3.3 左侧持久化侧栏（重点改造）

| # | 修改项 | 当前 | 目标 | 说明 |
|---|--------|------|------|------|
| 1 | 展现方式 | 抽屉式（点击打开，覆盖层） | 固定宽度常驻（280~320px） | 不再有"打开/关闭"操作 |
| 2 | 背景遮罩 | 有drawer-backdrop | 不需要 | 非抽屉模式 |
| 3 | Tabs结构 | [画图历史] [设置] 切换 | 上下分区：上半历史列表 + 下半设置面板（或保留Tabs） | 两种方案都可，优先分区避免切换 |
| 4 | 搜索框 | 无 | 增加会话搜索框 | 历史多了后快速定位 |
| 5 | 新建按钮 | Tab页内顶部 | 侧栏顶部显眼位置（独立一行） | 增加点击区域 |
| 6 | 会话卡片样式 | Card组件（白底+边框） | 行样式（hover高亮，选中态左侧色条） | 更紧凑，减少视觉噪音 |
| 7 | 设置项 | 仅字体大小 | 增加：主题切换、退出登录等 | |

### 3.4 中间结果展示区

| # | 修改项 | 当前 | 目标 | 说明 |
|---|--------|------|------|------|
| 1 | 次工具栏 | 无（选择模式时才有） | 常驻工具栏：新建/批量管理/下载全部 | 快捷操作入口 |
| 2 | 消息间距 | 较宽松（移动端间距） | 更紧凑的消息间距（margin-top: 12px → 8~10px） | 同屏显示更多内容 |
| 3 | 结果图片网格 | 动态列数（最多5列，单图大布局） | 根据宽度自适应：2~4列，图片卡片尺寸统一 | PC屏宽适合3~4列 |
| 4 | 图片卡片悬浮态 | 仅"保存"按钮常驻角标 | hover显示操作层：放大预览/下载/复制提示词/设为参考图 | 减少常驻视觉元素 |
| 5 | 配置信息标签 | 用户气泡底部一行灰色小字 | 更紧凑的标签展示或折叠到详情面板 | 减少对提示词阅读的干扰 |
| 6 | 操作按钮区 | 结果摘要下一排Button | 更紧凑：图标+文字或icon-only | 减少垂直占用 |
| 7 | 空状态插画 | Card + SVG图标 | 更大更美观的空状态插画 + 快捷提示词示例 | 引导用户上手 |

### 3.5 底部配置输入区（重点改造）

| # | 修改项 | 当前 | 目标 | 说明 |
|---|--------|------|------|------|
| 1 | 参数面板 | Collapse默认折叠，展开后横向滚动 | **默认展开**，选项网格布局（2行或换行） | PC端空间够，不需要折叠 |
| 2 | 参数项排列 | flex横向单行，溢出滚动 | grid或flex-wrap，所有选项一屏可见 | 消除横向滚动 |
| 3 | 参数项标签 | 中文标签（模型/模式/尺寸） | 保留标签，Select宽度适度加大 | 更容易选中 |
| 4 | 参考图卡片 | 全信息卡片（56px缩略图+文件名+删除按钮） | 紧凑缩略图横排（48~56px方形 + 删除角标） | 节省空间，只看视觉 |
| 5 | 参考图上传入口 | 仅输入区📎按钮 | 增加拖拽区，可拖入文件到输入区 | 更方便PC操作 |
| 6 | 提示词输入框 | 最小44px高，单列布局 | 最小80~100px高，支持多行展开 | PC端适合更长的提示词编辑 |
| 7 | 发送按钮 | 橙色🎨生成按钮 | 主按钮 + 快捷键提示（Shift+Enter） | 支持键盘操作 |
| 8 | 等待任务条 | 输入区顶部单行 | 保留，或移到顶部状态栏 | 保持可见即可 |
| 9 | 定位方式 | 绝对定位悬浮 + 消息列表padding预留 | Grid布局固定底区域，消息列表独立滚动 | 避免动态计算padding |
| 10 | 滚动收起FAB | 有（滚动时收起输入框，显示FAB） | 无。PC端保持输入区常驻，不需要收起 | 屏幕高度足够 |

---

## 四、暗黑主题样式适配要点

### 4.1 整体背景
| 区域 | 当前（动森） | 暗黑主题 |
|------|-------------|----------|
| 页面底背景 | 米色渐变 + 径向光斑（黄/绿） | 纯深色渐变：`#0d0d0f → #161618` 或低饱和蓝紫渐变 |
| 侧栏背景 | 浅米白渐变 | `#141418` 略浅于主背景，或同色但细边框分隔 |
| Header背景 | 奶白色渐变 | `#121216` + 底部1px边框线 |
| 结果卡片气泡 | `rgb(247, 243, 223)` 浅米黄 | `#1e1e22` + 1px `rgba(255,255,255,0.06)` 边框 |
| 用户气泡 | 青绿渐变（文字白） | 深青绿渐变或品牌色深色版，保持文字可读 |

### 4.2 文字与线条
| 元素 | 当前 | 暗黑 |
|------|------|------|
| 主文字 | `#794f27` 深棕 | `#e8e8ec` 近白 |
| 次要文字 | `#8a7b66` 棕灰 | `#a0a0aa` 中灰 |
| 辅助文字 | `#9f927d` 浅棕 | `#6b6b76` 暗灰 |
| 分割线/边框 | 浅棕色半透明 | 白色半透明 `rgba(255,255,255,0.06~0.1)` |
| 输入框placeholder | 浅灰 | `#555560` |

### 4.3 品牌色与功能色
| 色名 | 动森 | 暗黑 | 说明 |
|------|------|------|------|
| 品牌主色（brand） | `#19c8b9` | `#38e8d9` | 暗黑主题更亮、更饱和的青 |
| 强调色（accent） | `#f5c31c` 金黄 | `#ffcf3d` | 略提亮，保证暗底可见 |
| 危险色（danger） | `#e05a5a` | `#ff5c5c` | 略提亮 |
| 成功色（success） | `#3aa76d` | `#4ade80` | 略提亮 |

### 4.4 组件级暗黑适配清单

| 组件 | 需要特殊处理 |
|------|-------------|
| 生成结果图片卡片 | 增加图片外发光或深色内边框，避免白底图和背景融为一体 |
| animal-island-ui Card/Button/Select/Tabs | 写覆盖样式，覆盖组件库内部硬编码的浅色系 |
| 待处理上传缩略图 | 处理中状态改为蓝色发光动画代替浅色背景 |
| Select下拉选项列表 | 下拉弹出层必须是深色底白字 |
| 错误/警告Banner | 保持红色/黄色，但调整透明度避免刺眼 |
| Loading动画 | logo加载图：`logo-2.png` 在深色背景下需确保边缘清晰（或换色版本） |
| 图片空状态插画 | 颜色适配：SVG改为浅色描边，避免暗底不可见 |

---

## 五、用户体验优化点（附加建议）

### 5.1 交互增强
1. **拖拽生成**：结果图可拖拽到参考图区域作为新参考图
2. **右键菜单**：图片右键支持"复制图片/复制提示词/打开原图/下载"
3. **快捷键支持**：
   - `Ctrl/Cmd + Enter` 生成
   - `Ctrl/Cmd + N` 新建会话
   - `Ctrl/Cmd + S` 下载当前选中图片
   - `/` 聚焦提示词输入框
4. **批量操作**：选中多张图，批量下载到zip

### 5.2 信息层级优化
1. 当前结果卡片摘要（X/Y完成）颜色区分成功/失败/进行中
2. 生成耗时信息放在卡片底部小字，默认不突出
3. 用户提示词超长时默认截断，hover展开或"更多"按钮

---

## 六、实施优先级

| 优先级 | 模块 | 说明 |
|-------|------|------|
| P0 | 布局框架改造 | 从抽屉+悬浮改为固定三栏Grid布局 |
| P0 | 底栏参数面板常驻展开 | 消除Collapse，改为网格布局 |
| P0 | 左侧栏持久化 | 去掉抽屉机制 |
| P1 | Header合并余额+主题入口 | 紧凑顶部 |
| P1 | 结果图片卡片样式优化 | hover操作层 |
| P1 | 主题切换功能 | 动森↔暗黑变量覆盖 |
| P2 | 交互增强（快捷键/拖拽） | 附加体验优化 |
| P2 | 空状态美化 | 引导插画 |
| P2 | animal-island-ui组件深度覆盖 | 修复可能残留的浅色样式 |
